<h1>Events</h1>

<div id="events">
  <% @events.each do |event| %>
    ID: <%= event.id %>
    <p>
      <%= link_to "Show this event", event %>
    </p>
  <% end %>
</div>

<%= link_to "New event", new_event_path %>
